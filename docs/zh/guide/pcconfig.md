# 软件配置

在桌面端配置 > 更多配置中有软件配置项，可以配置是否开机自启动、启动密码等

## 禁止右键

开启后，就可以禁用右键了，但是在文本输入框中还是可以右键操作的，例如复制黏贴等操作

## 开机自启动

暂时仅 Electron 版本生效，打包后的软件在用户安装后就会开机自启动

## 启动方式

可以配置启动需不需要密码，可以每次都需要，也可以只需要输入正确一次，每次都需要就是说每次打开软件都需要重新输入密码，只需要一次就是说只要输入正确一次，后面再启动就不需要密码了。

可以编辑密码验证页面的主题样式，卡片样式，标题或者提示内容还有按钮等
